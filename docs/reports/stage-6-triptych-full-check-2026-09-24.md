# Stage 6 Triptych native/WASM release checkpoint

**Date:** 2026-09-24  
**Status:** passed for the WASM and macOS-native software targets  
**Triptych revision:** `44ba31043fbce95df82bb4e07ea607c8c03c688d`

This checkpoint records the complete Triptych repository check after the
Atom, Nucleus and Skate replacement-host proofs were added. ESP32 is not part
of this gate.

## Reproduction

```text
cd /Users/johnhardy/projects/triptych
npm run check
```

The command completed successfully on macOS. It included:

- TypeScript, lint, formatting and 676 unit/integration tests;
- 245 Playwright browser tests, including disk-library, reset/recovery,
  mobile-terminal, cross-tab ownership and public distribution flows;
- CP/M headless scenarios, ATOM/EDIT/NUC guest workflows and fresh-process
  native/WASM disk parity;
- the generated Skate program through native Rust and WASM hosts;
- large-drive and two-drive limits, arena limits, persistence lifetimes and
  native A/B PTY qualification;
- Rust formatting, Clippy with warnings denied, the complete Rust workspace
  test suite and the release `wasm32-unknown-unknown` build.

The measured software check therefore qualifies the same Triptych guest
workflow on both accepted machine targets. It is host-model evidence, not a
physical ESP32 measurement.

## Companion repository gates

The same checkpoint was followed by these clean local gates:

| Repository        | Gate                            | Result                                                           |
| ----------------- | ------------------------------- | ---------------------------------------------------------------- |
| Atom              | `npm run release:check`         | 369 tests, native self-host and no-AZM build measurements passed |
| Nucleus           | `npm run typecheck && npm test` | 150 tests passed                                                 |
| z80-tool-services | `npm run check`                 | 132 tests, package build and smoke passed                        |

The Atom and Nucleus Triptych-WASM adapters remain opt-in host adapters. Their
reports compare generated bytes, service traces and cycle totals with the
Debug80 reference without importing Debug80 into Triptych production code.

## Remaining gates

This does not claim that the active Nucleus `compiler-rewrite-12k` line has
completed its ATOM source/image reconciliation. Its generated compiler-image
path still has the historical AZM exception documented in the Stage 2 report.
The remaining platform work is therefore:

1. selectively port and qualify that active Nucleus line with ATOM;
2. widen the shared running-program service profile where a second real
   consumer requires it; and
3. repeat the Debug80 consumer audit before retiring any reference adapter.

No ESP32 build, boot or timing evidence is required to close this software
checkpoint.
